spring:
  application:
    name: registration-service

server:
  port: 8085

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}

keycloak:
  realm: ${REALM:taxi_realm}
  server-url: ${KEYCLOAK_SERVER_URL}
  admin-client-id: ${ADMIN_ClIENT_ID:admin-cli}
  admin-client-secret: ${ADMIN_CLIENT_SECRET:rNATOcv9HUDGj9f7fEP58TzvZ3xIKB5H}